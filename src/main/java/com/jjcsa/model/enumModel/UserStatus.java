package com.jjcsa.model.enumModel;

public enum UserStatus {
    Pending, // When user signs up
    NewUser, // When user is approved
    Active, // When user finishes on-boarding profile
    Rejected // When user is rejected
}
